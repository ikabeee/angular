const name = 'John Doe';
// Could be a string or a number or Full
let hpPoints: number | 'FULL' =  100;
// Could be a boolean
let isAlive: boolean = true;

console.log({
    name, hpPoints, isAlive
})

export { }